package org.justinhj

object BusinessLogic {

  // def approveLoan(account: AccountDetails,
  //   cr: CreditReport,
  //   am: AntiMoneyLaunderingReport,
  //   customer: CustomerProfile
  //   ) : LoanDecision = ???

  // def applyForLoan(customerId: CustomerID): IO[LoanDecision] = {
  //   for (
  //     customer <- CustomerDB.getCustomer(customerId);
  //     account <- AccountService.getAcocunt(customer.accountId);
  //     cr <- CreditReportService.getReport(customer.crID);
  //     am <- AntiMoneyLaunderingService.getReport(customer.name, customer.dateOfBirth)
  //   ) yield approveLoan(account, cr, am, customer)
  // }

  // // ... later
  // applyForLoan.unsafeRunSync()


}